define([],function(){
	
	return {
		const init	 =	function(){
			window.addEventListener('hashchange',function(e) { 
				hashMap(e.newURL);
			},false);
		},
		const hashMap= function(){
			
		}，
		const go = function(url,input_param){
			
		}
	}
})